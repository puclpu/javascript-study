// ES6

// 1. let(변수), const(상수)

// 2. arrow function
function add() {}

var add = function () {};

var add = (x) => {
    return 1;
}

var add = (x) => 1;

var add = x => 1;

// 3. 삼항 연산자
// condition ? true일 경우 : false일 경우